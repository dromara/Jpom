--  创建表
CREATE TABLE IF NOT EXISTS PUBLIC.USEROPERATELOGV1
(
    IP        VARCHAR(30),
    USERID    VARCHAR(30),
    RESULTMSG VARCHAR(1000000000),
    OPTTYPE   INTEGER,
    OPTSTATUS INTEGER,
    OPTTIME   BIGINT,
    NODEID    VARCHAR(30),
    REQID     VARCHAR(50),
    DATAID    VARCHAR(50)
);

--  添加列
ALTER TABLE USEROPERATELOGV1
    ADD IF NOT EXISTS USERAGENT VARCHAR(300);